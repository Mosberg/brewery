package dk.mosberg.block;

import net.minecraft.block.Block;

public class BottlerBlock extends Block {
    public BottlerBlock(Settings settings) {
        super(settings);
    }
    // Add custom logic for bottler if needed
}
