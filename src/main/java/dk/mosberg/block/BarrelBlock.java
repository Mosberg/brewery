package dk.mosberg.block;

import net.minecraft.block.Block;

public class BarrelBlock extends Block {
    public BarrelBlock(Settings settings) {
        super(settings);
    }
    // Add custom logic for barrel if needed
}
