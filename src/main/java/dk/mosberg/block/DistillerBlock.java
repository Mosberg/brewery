package dk.mosberg.block;

import net.minecraft.block.Block;

public class DistillerBlock extends Block {
    public DistillerBlock(Settings settings) {
        super(settings);
    }
    // Add custom logic for Distiller if needed
}
