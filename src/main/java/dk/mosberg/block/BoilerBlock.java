package dk.mosberg.block;

import net.minecraft.block.Block;

public class BoilerBlock extends Block {
    public BoilerBlock(Settings settings) {
        super(settings);
    }
    // Add custom logic for boiler if needed
}
